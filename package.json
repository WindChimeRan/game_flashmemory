{
  "name": "oom",
  "version": "0.1.0",
  "description": "Terminal arcade game: you are the KV-cache eviction & prefetch policy. Tetris, except the falling blocks are context, and the loss condition is OOM.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "play": "bun src/cli.ts play",
    "sim": "bun src/cli.ts sim",
    "demo": "bun src/cli.ts demo",
    "gates": "bun scripts/gates.ts"
  },
  "devDependencies": {
    "@types/node": "^22",
    "bun-types": "^1.3.0",
    "typescript": "^5.7.0"
  }
}
